/* BY: Melvin Peralta
 * Tutorial: Lazy foo SDL tutorial
 * there will most likely be differences between tutorial code
 *  and my code
 */
#include <SDL2/SDL.h>
#include <stdio.h>

int main(int argc, char *args[]){
    // init a window to null
    SDL_Window *window = nullptr;
    // surface? w/e that is also init to null
    SDL_Surface *screenSurface = nullptr;

    
}